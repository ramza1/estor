<% if @shop.errors.any? %>
    <div class="alert alert-error" style="margin-top: 10px;">
      <h4><%= pluralize(@shop.errors.count, "error") %> prohibited your shop from being saved:</h4>

      <ul>
        <% @shop.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>