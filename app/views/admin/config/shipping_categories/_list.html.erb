<table class="table table-striped table-bordered dataTable" style="margin-top: 10px;">
  <thead>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @shipping_categories.each do |shipping_category| %>
      <tr>
        <td><%= link_to shipping_category.name, edit_admin_config_shipping_category_url(shipping_category) %></td>
        <td>
          <div class="">
            <%= link_to  edit_admin_config_shipping_category_url(shipping_category), class: "btn btn-small" do %>
                <i class="icon-edit"></i>  Edit
            <% end %>
          </div>
        </td>

      </tr>
  <% end %>
  </tbody>
</table>
