<table class="table table-striped table-bordered dataTable" style="margin-top: 10px;">
  <thead>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @shipping_zones.each do |shipping_zone| %>
      <tr>
        <td><%= link_to shipping_zone.name, edit_admin_config_shipping_zone_url(shipping_zone) %></td>
        <td>
          <div class="">
            <%= link_to  edit_admin_config_shipping_zone_url(shipping_zone), class: "btn btn-small" do %>
                <i class="icon-edit"></i>  Edit
            <% end %>
          </div>
        </td>

      </tr>
  <% end %>
  </tbody>
</table>
