<table class="table table-striped table-bordered dataTable">
  <thead>
  <tr class='odd'>

    <th  class='column1_header'>Name</th>
    <th>Product</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% for image_group in @image_groups %>
      <tr  class='<%= cycle("odd", "")%>'>
        <td><%= link_to image_group.name, [:admin, image_group] %></td>
        <td><%= image_group.product.name %></td>
        <td><%= link_to "edit", edit_admin_image_group_path(image_group) %></td>
      </tr>
  <% end %>
  </tbody>
</table>