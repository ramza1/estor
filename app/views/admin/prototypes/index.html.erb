<%= content_for(:breadcrumb) do %>
    <li>
      <%= link_to "Dashboard", admin_root_url %>
      <span class="icon-angle-right"></span>
    </li>
    <li>
      <%= link_to_unless_current "Prototypes", admin_prototypes_path %>
    </li>
<% end %>

<% content_for(:page_title) do %>
    Manage Prototypes
<% end %>

<div class="row-fluid">
  <div class="span12">
    <div class="social-box">
      <div class="header">
        <div class="btn-group hidden-phone">
          <%= link_to "Create a new Prototype", new_admin_prototype_url, class: "btn btn-primary" %>
        </div>
      </div>
      <div class="body">
        <% if @prototypes.empty? %>
            <p>
              No Prototypes
            </p>
        <% else %>
            <%= render "list" %>
        <% end %>
      </div>
    </div>
  </div>
</div>