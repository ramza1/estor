<%= content_for(:breadcrumb) do %>
    <li>
      <%= link_to "Dashboard", admin_root_url %>
      <span class="icon-angle-right"></span>
    </li>
<% end %>


<div class="row-fluid">
  <div class="span12">
    <div class="social-box">
      <div class="header">
        <h4>Deals</h4>
        <div class="btn-group hidden-phone pull-right">
          <%= link_to "New Deal", new_admin_generic_deal_path, class: "btn btn-primary" %>
        </div>
      </div>
      <div class="body">
        <% if @deals.empty? %>
            <p class="alert alert-info">
              No Deal Available Yet
            </p>
        <% else %>
            <%= render "list" %>
        <% end %>

        <%= will_paginate @deals %>
      </div>
    </div>
  </div>
</div>



