<% if @brand.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

    <ul>
    <% @brand.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.input :name, input_html: {class: "span7"} %>
  <%= f.input :icon, as: :file, input_html: {class: "input-xlarge"} %>
</div>
<div class="form-actions">
  <%= submit_tag "Create Brand", :class => "btn" %>
</div>
