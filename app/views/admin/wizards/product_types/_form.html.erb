<% if @product_type.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@product_type.errors.count, "error") %> prohibited this product_type from being saved:</h2>

    <ul>
    <% @product_type.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form.input :name, input_html: {class: "span9"} %>

<div class="control-group">
  <%= form.label :parent, class: " control-label" %>
  <div class="controls">
    <%= form.collection_select :parent_id, ProductType.all, :id, :name, include_blank: true, class: "input-xlarge" %>
  </div>
</div>


<div class="form-actions">
  <%= form.submit 'Create', :class => 'button', :style => "margin-top: 10px" %>
</div>

