<% if target.errors.any? %>
    <div class="alert alert-error">
    <h4><span class="icon"><i class="icon-warning-sign"></i></span>
      <%= pluralize(target.errors.count, "error") %> prohibited this <%= target.class.name.downcase %> form being saved
    </h4>
   	<% target.errors.full_messages.each do |msg| %>
      <ul>
        <li>
          <%= msg %>
        </li>
      </ul>
	<% end %>
    </div>
<% end %>